[package]
name = "audio-modem-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Experimental PCM and WAV codec for AudioModem"

[lints]
workspace = true

[dependencies]
adlp-protocol = { path = "../adlp-protocol" }
